using UnityEngine;

public class AttachableItem : MonoBehaviour
{
    // This script acts as a marker for now.
    // You can add properties later if needed (e.g., for size constraints or attachment offsets).
}